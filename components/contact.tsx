'use client'

import { useState } from 'react'
import { motion } from 'framer-motion'
import { Button } from '@/components/ui/button'
import { Phone, Mail } from 'lucide-react'

export function Contact() {
  const [formData, setFormData] = useState({ name: '', phone: '', email: '', area: '', vehicle: '', service: '', dateTime: '', notes: '' })
  const [isSubmitting, setIsSubmitting] = useState(false)
  const [submitSuccess, setSubmitSuccess] = useState(false)
  const services = ['Full Detail', 'Interior Detail', 'Exterior Detail']
  const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement>) => setFormData((prev) => ({ ...prev, [e.target.name]: e.target.value }))
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setIsSubmitting(true); await new Promise((resolve) => setTimeout(resolve, 1000)); setSubmitSuccess(true); setIsSubmitting(false); setTimeout(() => { setSubmitSuccess(false); setFormData({ name: '', phone: '', email: '', area: '', vehicle: '', service: '', dateTime: '', notes: '' }) }, 3000) }
  if (submitSuccess) return <section id="contact" className="flex min-h-screen items-center justify-center bg-gray-100 py-20"><div className="rounded-2xl border-2 border-blue-600 bg-white p-8 text-center"><h3 className="mb-3 text-3xl font-bold text-black">Thanks for your request!</h3><p className="text-gray-700">We&apos;ve received your booking request and will be in touch soon.</p></div></section>
  return <section id="contact" className="bg-gray-100 px-4 py-16 sm:px-6 sm:py-20 lg:px-8 lg:py-24"><div className="mx-auto max-w-4xl"><motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="mb-12 text-center"><h2 className="mb-4 text-4xl font-bold text-black sm:text-5xl">Book Your Detail</h2><p className="text-lg text-gray-700">Tell us about your vehicle and preferred time. We&apos;ll confirm the details with you.</p></motion.div><div className="mb-12 grid gap-6 sm:grid-cols-2"><div className="rounded-lg border-2 border-blue-600 bg-white p-6"><div className="flex gap-4"><Phone className="h-6 w-6 text-blue-600" /><div><h3 className="font-bold text-black">Phone</h3><a href="tel:+18043006441" className="font-semibold text-blue-600">(804) 300-6441</a></div></div></div><div className="rounded-lg border-2 border-blue-600 bg-white p-6"><div className="flex gap-4"><Mail className="h-6 w-6 text-blue-600" /><div><h3 className="font-bold text-black">Email</h3><a href="mailto:info@qcautodetailing.com" className="font-semibold text-blue-600">info@qcautodetailing.com</a></div></div></div></div><motion.form onSubmit={handleSubmit} className="space-y-6 rounded-2xl bg-white p-8 sm:p-12"> <p className="rounded-lg border border-blue-200 bg-blue-50 p-4 text-sm leading-relaxed text-gray-700"><strong>Before we arrive:</strong> Please have access to an outdoor water source and a nearby electrical outlet for our equipment.</p><div className="grid gap-6 sm:grid-cols-2">{[['name','Name','Your name'],['phone','Phone','(804) 555-0147'],['email','Email','your@email.com'],['area','City / Area','e.g., Richmond, Henrico'],['vehicle','Vehicle (Year / Make / Model)','e.g., 2022 Honda Civic']].map(([name,label,placeholder]) => <div key={name}><label className="mb-2 block font-semibold text-black">{label}</label><input type={name === 'email' ? 'email' : name === 'phone' ? 'tel' : 'text'} name={name} value={formData[name as keyof typeof formData]} onChange={handleChange} required className="w-full rounded-lg border-2 border-gray-300 px-4 py-3 text-black focus:border-blue-600 focus:outline-none" placeholder={placeholder} /></div>)}<div><label className="mb-2 block font-semibold text-black">Service</label><select name="service" value={formData.service} onChange={handleChange} required className="w-full rounded-lg border-2 border-gray-300 px-4 py-3 text-black focus:border-blue-600 focus:outline-none"><option value="">Select a service</option>{services.map((service) => <option key={service}>{service}</option>)}</select></div></div><div><label className="mb-2 block font-semibold text-black">Preferred Date / Time Window</label><input name="dateTime" value={formData.dateTime} onChange={handleChange} required className="w-full rounded-lg border-2 border-gray-300 px-4 py-3 text-black focus:border-blue-600 focus:outline-none" placeholder="e.g., Saturday afternoon" /></div><div><label className="mb-2 block font-semibold text-black">Notes About Condition</label><textarea name="notes" value={formData.notes} onChange={handleChange} rows={4} className="w-full resize-none rounded-lg border-2 border-gray-300 px-4 py-3 text-black focus:border-blue-600 focus:outline-none" placeholder="Pet hair, stains, or other details..." /></div><Button type="submit" disabled={isSubmitting} className="w-full bg-blue-600 py-3 text-lg font-semibold text-white hover:bg-blue-700">{isSubmitting ? 'Submitting...' : 'Request Booking'}</Button></motion.form></div></section>
}
